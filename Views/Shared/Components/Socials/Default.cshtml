@model IList<Social>

<div class="socials">
    @for (int i = 0; i < Model.Count(); ++i)
    {
        if (Model[i].Url is not null)
        {
            <a href="@Model[i].Url">
                <div class="@Model[i].FontClass">
                </div>
            </a>
        }
    }
</div>